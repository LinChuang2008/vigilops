apiVersion: v1
kind: Service
metadata:
  name: {{ include "nightmend.fullname" . }}-backend
  labels:
    {{- include "nightmend.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nightmend.backend.selectorLabels" . | nindent 4 }}
