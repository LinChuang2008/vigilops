apiVersion: v1
kind: Service
metadata:
  name: {{ include "nightmend.fullname" . }}-frontend
  labels:
    {{- include "nightmend.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nightmend.frontend.selectorLabels" . | nindent 4 }}
