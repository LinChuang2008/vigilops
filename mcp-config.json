{
  "mcpServers": {
    "vigilops": {
      "command": "python",
      "args": ["-m", "app.mcp.cli"],
      "cwd": "./backend",
      "env": {
        "DATABASE_URL": "postgresql://vigilops:vigilops123@localhost:5433/vigilops",
        "REDIS_URL": "redis://localhost:6380"
      }
    }
  }
}